# Uso de inteligencia artificial

## Alcance declarado en la Iteración 1

Se utilizó IA como apoyo de programación en pareja y documentación para revisar la estructura de la solución, contrastar los flujos de proveedores entre capas, proponer casos de prueba, ejecutar verificaciones y mantener los documentos técnicos alineados con el repositorio.

## Módulos asistidos

- Proveedores en Domain y Application: normalización, validación, duplicidad, consulta, edición, concurrencia y baja lógica.
- Persistencia: mapeos de EF Core, migraciones, índice único parcial y filtro de bajas.
- Entradas HTTP: API REST y MVC de proveedores.
- Pruebas: revisión de escenarios unitarios e integrados con PostgreSQL real.
- Documentación: arquitectura, datos, API, pruebas, Docker y bitácora XP.
- Licitaciones y ofertas (Iteración 2): análisis de código de dominio (`Licitacion.Publicar`, `LicitacionTransicion`) para la fase refactor de HU-11; refactors de HU-12 y HU-13; y refactor de HU-14 para sustituir la comparación textual de errores por `OfertaDuplicadaException`, acotar la traducción de PostgreSQL al índice esperado y centralizar el mapeo `OfertaDto`. En HU-15, Codex apoyó la creación y ejecución de las pruebas ROJO, el diagnóstico del comportamiento esperado, la revisión del VERDE aportado por la pareja y el refactor de nombres y construcción duplicada de excepciones. En HU-16 ayudó a inspeccionar la cobertura previa, crear las pruebas Application/HTTP, verificar el VERDE con PostgreSQL real y refactorizar la calculadora pura y las aserciones sin cambiar comportamiento. En HU-17 apoyó la inspección de cobertura, las pruebas HTTP, la verificación del VERDE, la eliminación de la proyección duplicada de ofertas y el diagnóstico de Testcontainers que llevó a compartir una sola fixture PostgreSQL entre las pruebas integradas. La verificación final alcanzó 175 pruebas verdes y la documentación se contrastó con código, CI y commits reales. En el cierre documental del 20 de agosto de 2026 la IA apoyó, sin modificar código: verificar con `git log main --merges` que los PR #19 a #26 fusionaron las ocho historias; consultar la API pública de GitHub para confirmar el resultado de CI en cada commit de fusión; evaluar la Definition of Done por historia (excluyendo HU-12 de la velocidad observada por carecer de endpoints HTTP y registro DI); calcular la velocidad observada (26 SP); y realinear `docs/modulos/ofertas.md` con lo implementado por HU-17. Cada dato se contrastó con commits, ejecuciones de CI, controladores y archivos reales antes de registrarse.
- Regularización retrospectiva de trazabilidad de Iteración 1: Codex auditó HU-00 a HU-09, la renumeración histórica de proveedores, los PR #2 a #17 aplicables, commits, pruebas y ejecuciones CI. Creó los Issues retrospectivos #37 a #45 para las historias faltantes y conservó #8 como referencia histórica equivalente a HU-06, añadiendo solo comentarios aclaratorios. La intervención ocurrió después del cierre y de `v0.1.0`, sin modificar código, pruebas, commits, PR, el Issue histórico ni etiquetas.
- Regularización retrospectiva de trazabilidad de Iteración 2: Codex auditó la ausencia de Issues para HU-10 a HU-17, contrastó historias, prioridades y criterios con `historias-usuario.md`, verificó ramas, commits, pruebas, PR #19 a #26, correcciones del PR #28 y ejecuciones reales de GitHub Actions. Después del cierre y de la creación de `v0.2.0`, creó los Issues retrospectivos #29 a #36, añadió comentarios explícitos a los PR y actualizó únicamente documentación. La intervención no modificó código, pruebas, commits históricos, ramas remotas ni etiquetas.
- HU-18 (Iteración 3): la IA apoyó exclusivamente la fase refactor y la documentación. En el refactor propuso mejoras acotadas sin comportamiento nuevo: renombrar el método redundante `ResolverNivelAprobacion` a `ResolverAsync`, alinear el helper del controlador con la convención `CrearProblema` y limpiar una línea sobrante; verificó build (0 errores, 0 advertencias) y ejecutó la suite completa antes y después (187 pruebas verdes). Reportó como candidatos a Issues separadas —sin ocultarlos en la historia— la duplicación de `CrearProblema` en cuatro controladores, la ausencia de unit tests de los dos servicios de HU-18 y la dependencia del resolutor sobre `ILicitacionConsultaRepository`. En la documentación redactó `docs/modulos/niveles-aprobacion.md` y actualizó `docs/api.md`, `docs/modelo-datos.md`, `docs/pruebas.md` y `docs/bitacora-xp.md`, contrastando cada dato con los commits `bd1f3d6`, `249ab70` y `1224ece`, las ejecuciones reales de GitHub Actions y el código fusionado; no marcó criterios ni cerró la Issue #47.
- HU-19 (Iteración 3): la IA apoyó la fase refactor y la documentación. En el refactor, con la suite previamente verde (191 pruebas), propuso mejoras sin comportamiento nuevo: centralizar el par USD/CRC en constantes del dominio (`MonedaOrigenPredeterminada`/`MonedaDestinoPredeterminada`), eliminar la consulta duplicada del tipo de cambio activo en `IOfertaConsultaRepository`/`OfertaRepository` consolidando `ITipoCambioRepository.ObtenerActivoAsync` como única fuente y simplificar `ConvertirAsync`; verificó build (0 errores, 0 advertencias), formato sin diferencias y ejecutó la suite completa antes y después (191 verdes). Reportó como candidato a Issue separada —sin ocultarlo— el mojibake preexistente de HU-17 en los mensajes de `ValidarConsulta`, y anotó que el helper `CrearProblema` pasó a cinco copias. En la documentación redactó `docs/modulos/tipo-cambio.md` y actualizó `docs/api.md`, `docs/modelo-datos.md`, `docs/pruebas.md` y `docs/bitacora-xp.md`, contrastando cada dato con los commits `78be404` (rojo, CI fallido como es esperable), `7ab8e09` (verde, CI success), el commit local `ff92f44` —sin push ni CI todavía, registrado como tal— y las cuatro pruebas HTTP reales de HU-19; no marcó criterios ni cerró la Issue #48.
- HU-20 (Iteración 3): la IA apoyó la verificación del VERDE, la evaluación del refactor y la documentación; las pruebas funcionales y la vista landing fueron aportadas por la pareja en el commit `8062619`. La IA confirmó que las pruebas específicas y la suite completa estaban en verde antes de intervenir (193 pruebas con `dotnet test Licitaciones.sln`). En el refactor intentó eliminar la duplicación de los cinco bloques de tarjeta mediante un bucle Razor, diagnosticó con la prueba fallida que `HtmlEncoder.Default` escapa los caracteres fuera de Basic Latin («licitaci&#243;n») y revirtió el cambio sin retener código, descartando también `Html.Raw` y la reconfiguración global del encoder como compensaciones peores; no fabricó commit de refactor. Reportó como candidatos a Issues separadas —sin ocultarlos— la triplicación del setup de `WebApplicationFactory` en las pruebas funcionales, el `lang="en"` del layout frente al contenido en español y la publicación del ciclo TDD en un único commit sin evidencia separada de ROJO. En la documentación actualizó `docs/modulos/interfaz-web.md`, `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo, contrastando cada dato con el commit `8062619`, el PR #60 y su ejecución de CI `32613192010` consultadas en la API pública de GitHub; no marcó criterios ni cerró la Issue #49.
- HU-21 (Iteración 3): Codex apoyó la verificación de la línea base verde, la evaluación del refactor y la trazabilidad documental. En el refactor redujo la duplicación de los seis enlaces MVC del partial de navegación mediante una colección de rutas y etiquetas, conservando el resaltado activo, el enlace a Swagger y todos los criterios de la Issue #50; la prueba específica terminó con 6 casos correctos y la suite completa con 199. En la documentación actualizó `docs/modulos/interfaz-web.md`, `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo, contrastando los commits `a9dd711` (ROJO), `0e226e7` (VERDE), `b206f9c` (ajuste de estilo) y `e2fbd06` (REFACTOR) con el historial y las pruebas locales. No se registró PR de HU-21 ni se marcó o cerró la Issue #50.
- HU-22 (Iteración 3): Codex apoyó las tres fases y la trazabilidad documental. En ROJO contrastó la Issue #51 con `historias-usuario.md` antes de programar, verificó la ausencia de escenarios duplicados y creó `TemaClaroOscuroWebTests` (cinco casos funcionales HTTP, commit `12aa5c4` de la pareja), confirmando el estado rojo por comportamiento ausente (5 fallidas). En VERDE verificó la implementación aportada por la pareja (`9975a05`): ejecución filtrada 5 correctas y suite completa en verde. En REFACTOR eliminó la aplicación inicial del tema duplicada entre el script del layout y `site.js`, inlineó el helper de un solo uso, renombró una variable y añadió el salto final de `site.css`, sin comportamiento nuevo ni commit; reportó como candidatos a Issues separadas —sin ocultarlos— el ícono fijo del control que no refleja el tema activo, la duplicación de `PaginasDelSitio` entre pruebas de HU-21 y HU-22, y la discrepancia entre la rama prevista (`tema-claro-oscuro`) y la real (`modo-claro-oscuro`). La verificación posterior al refactor alcanzó 204 pruebas verdes. En la documentación actualizó `docs/modulos/interfaz-web.md`, `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo, contrastando cada dato con los commits, el PR #62 (abierto, mergeable, CI en verde) consultado en la API pública de GitHub y las ejecuciones locales; no marcó criterios ni cerró la Issue #51.
- HU-23 (Iteración 3): Codex apoyó la revisión de criterios de la Issue #52, la verificación de la línea base verde, la ejecución focalizada de las 12 pruebas CRUD web y la suite completa de 216 pruebas. En REFACTOR identificó que `PaginaResultado<T>` era un contrato compartido ubicado bajo el namespace de Proveedores y lo movió a `Licitaciones.Application.Common`, sin comportamiento nuevo; verificó compilación, las pruebas CRUD y la suite completa después del cambio. En la documentación actualizó `docs/modulos/interfaz-web.md`, `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo, contrastando los commits `b5ff1fe` (ROJO), `e4b7973` (VERDE), `5b3be34` (corrección) y `2803c00` (REFACTOR local) con las pruebas y el estado del PR #63. No modificó API, modelo de datos ni Docker, no atribuyó CI al refactor local y no marcó ni cerró la Issue #52.
- HU-24 (Iteración 3): Codex apoyó la fase ROJO, la verificación del VERDE, el REFACTOR y la trazabilidad documental. En ROJO contrastó la Issue #53 con `historias-usuario.md` antes de programar, verificó que ninguna prueba previa cubriera mensajería visible y creó `MensajeriaWebTests` (tres casos de integración HTTP con PostgreSQL real, commit `6b15bed` de Tiffany), confirmando el rojo por comportamiento ausente y corrigiendo un fallo artificial detectado (monto mayor al presupuesto sembrado) para que las 3 fallidas reflejaran solo lo esperado. En VERDE verificó la implementación aportada por la pareja (`e6213df`): filtro HU-24 con 3 correctas, suite completa en 219 verdes y CI `success`. En REFACTOR extendió el parcial `_Mensajes` a las seis vistas restantes con bloques duplicados, limpió los usings de `Program.cs`, ajustó la prueba estructural `CreateView_DebeRenderizarMensajesDeValidacionDelNombre` al nuevo formato sin reducir su intención y mantuvo suite y formato en verde sin hacer commit; reportó como candidatos a Issues separadas —sin ocultarlos— el mensaje invisible tras crear una licitación, el mensaje invisible tras editar un proveedor (redirige a `Details` sin parcial) y la variante de advertencia sin flujos productores. En la documentación actualizó `docs/modulos/interfaz-web.md`, `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo, contrastando cada dato con los commits reales, las ejecuciones locales y el estado del PR #64; no marcó criterios ni cerró la Issue #53.
- HU-25 (Iteración 3): Codex apoyó la fase ROJO, la verificación del VERDE, el REFACTOR y la trazabilidad documental. Antes de programar contrastó la Issue #54 con `historias-usuario.md` confirmando título, prioridad Baja, 1 SP y el criterio único; verificó que ninguna prueba previa cubriera formato monetario y creó `FormatoMonetarioWebTests` (tres casos de integración HTTP con PostgreSQL real, commit `857f458` de Seidy), confirmando el rojo por comportamiento ausente (`.ToString("N2")` sin cultura ni ₡ en las vistas) y corrigiendo un fallo artificial detectado (el rango sembrado traslapaba el nivel Directivo activo) desactivando temporalmente ese nivel del catálogo. En VERDE verificó la implementación aportada por la pareja (`f3ff76e`: helper `FormatoMonetario`, `UnicodeRanges.CurrencySymbols` en el encoder y `.Dinero()` en tres listados): filtro HU-25 con 3 correctas, suite completa en 222 verdes y CI `success` (`32673569441`) tras el rojo esperable (`32669889839`). En REFACTOR aplicó solo mejoras de legibilidad en las pruebas —literal ₡ e import de `Domain.Aprobaciones`, commit `4fd4175` local— evaluó el código de producción sin cambios justificados y reportó como candidatos a Issues separadas —sin ocultarlos— los montos de `NivelesAprobacion/Delete` aún con `ToString("N2")`, la duplicación del arranque de `WebApplicationFactory` entre clases de IntegrationTests y pruebas unitarias dedicadas al helper. En la documentación actualizó `docs/modulos/interfaz-web.md`, `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo, contrastando cada dato con los commits reales, las ejecuciones de CI consultadas en la API pública de GitHub y las ejecuciones locales; no marcó criterios ni cerró la Issue #54.
- HU-26 (Iteración 3): Codex apoyó la fase ROJO, la verificación del VERDE, el REFACTOR y la trazabilidad documental. Antes de programar contrastó la Issue #55 con `historias-usuario.md` confirmando título, prioridad Alta, 8 SP y los cinco criterios, y detectó dos desviaciones que reportó sin corregir en fase: la rama real (`iteracion-3/hu-26-api-rest`) difiere de la prevista y el commit VERDE quedó referenciado como `refs #56`. Inspeccionó la suite HTTP previa para no duplicar escenarios (DTOs, rutas versionadas, listados y códigos ya cubiertos por HU-10/14/15/16/17) y creó `ContratoApiRestHttpTests` con cinco pruebas del contrato transversal de errores (commit `9611c8d` de Tiffany), incluido un 500 determinístico saboteando `ConsultarProveedorService`; confirmó el rojo por comportamiento ausente (sin `codigoError`, sin `correlacionId` y 404 sin cuerpo; CI fallido esperable `32676486243`). En VERDE verificó la implementación aportada por la pareja (`7db6e80`: `FabricaProblemDetailsApi` como `ProblemDetailsFactory`, `RespuestaProblema` centralizado y manejador global que mapea `DomainException` a 422 y lo no previsto a un 500 controlado): filtro HU-26 con 6 correctas, suite completa en 227 verdes y CI `success` (`32677819388`). En REFACTOR extrajo `ContratoProblemasApi` como única fuente de las claves del contrato, delegaron en ella la fábrica y `RespuestaProblema`, reutilizó la fábrica dentro del manejador de excepciones eliminando la construcción manual duplicada y retiró un modificador `partial` sobrante; conservó los wrappers `CrearProblema` de una línea por ganancia marginal de su sustitución; sin comportamiento nuevo, suite en 227 verdes y formato sin diferencias. Reportó como candidatos a Issues separadas —sin ocultarlos— la referencia `#56` del commit VERDE, la discrepancia de nombre de rama, los residuos de plantilla `WeatherForecast` y la posible extensión sobre `ControllerBase`. En la documentación actualizó `docs/modulos/api-rest.md`, `docs/api.md`, `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo, contrastando cada dato con los commits reales, las ejecuciones de CI consultadas en la API pública de GitHub y las ejecuciones locales; no marcó criterios ni cerró la Issue #55.

- HU-27 (Iteración 3): Codex apoyó las tres fases y la trazabilidad documental. Antes de programar contrastó la Issue #56 con `historias-usuario.md` confirmando título, prioridad Media, 2 SP y los dos criterios; verificó que ninguna prueba previa cubriera documentación interactiva y creó `DocumentacionSwaggerHttpTests` (cuatro pruebas HTTP sobre Swagger UI y swagger.json) y `DocumentacionApiMarkdownTests` (dos unitarias sobre `docs/api.md`), confirmando el rojo observado: 4 HTTP fallidas por 404 y 1 unitaria por colección faltante —la otra pasó legítimamente porque `api.md` ya documentaba recursos y errores desde iteraciones previas—. En VERDE verificó la implementación aportada por la pareja (`b790880`: Swashbuckle 7.2.0, `AddSwaggerGen` con comentarios XML, middleware solo en Development, sección en `api.md` y colección `docs/api.http`), corrigió errores de compilación del filtro de ejemplos, lo extendió con los siete contratos de solicitud, restauró sin pérdida el `api.http` pisado accidentalmente durante la fase y deduplicó un párrafo repetido en `api.md`; filtro HU-27 con 6 correctas y suite completa en 233 verdes, con CI `success` (`32684426351`) tras el rojo esperable (`32682545858`). En REFACTOR sustituyó la ternaria de 11 ramas de `EjemplosEsquemasFiltro.Apply` por un diccionario estático tipo→ejemplo, renombró los métodos ambiguos y descartó extraer el bloque `AddSwaggerGen` por abstracción especulativa (commit local `14a8421`, sin push); reportó como candidato a Issue separada —sin ocultarlo— la consolidación de la fábrica `CrearApiFactory` duplicada entre las pruebas de HU-26 y HU-27. En la documentación actualizó `docs/modulos/api-rest.md`, `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo, contrastando cada dato con los commits reales, las ejecuciones de CI consultadas en la API pública de GitHub y el estado `ahead 1` de la rama local; no marcó criterios ni cerró la Issue #56.

- Cierre documental de la Iteración 3: Codex verificó sin modificar código que las diez historias HU-18 a HU-27 están fusionadas en `main` (PR #58 a #67; commits de fusión y ejecuciones de CI `success` —`32560841685` a `32688275482`— consultadas en la API pública de GitHub), que cada historia tiene exactamente una Issue (#47 a #56) cerrada inmediatamente después de su fusión sin cierres prematuros, y evaluó la Definition of Done por historia contra pruebas con trait propio, commits rojo/verde/refactor, documentación por historia y CI. Calculó la velocidad observada (38 SP frente a 36 planificados y 38 seleccionados), consolidó ciclos TDD, refactorizaciones y rotaciones Driver/Navigator (57 commits: Seidy 28, Tiffany 29), registró las observaciones de trazabilidad (referencia `refs #56` del commit VERDE de HU-26, cuatro ramas reales distintas de las previstas y la entrada desactualizada del PR de HU-21) y los ajustes para la Iteración 4 derivados de brechas verificables (mensajería invisible en dos flujos, variante de advertencia sin productores, ícono de tema estático, formato ₡ faltante en `NivelesAprobacion/Delete`, residuos `WeatherForecast`, duplicaciones de fábricas de pruebas). Actualizó `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo contrastando cada dato con `git log`, `git tag`, `gh issue/pr/run`, la suite local de 233 pruebas verdes y el formato verificado sobre el commit `666f175`; no creó la etiqueta `v0.3.0` ni modificó código.

- Inicio de la Iteración 4: Codex contrastó la ausencia de Issues para HU-28 a HU-37 con `historias-usuario.md`, creó las tarjetas de trazabilidad #69 a #78 desde el texto literal del catálogo (historia, prioridad, estimación, iteración, pareja, rama prevista y criterios sin marcar) y actualizó `docs/plan-xp.md`, `docs/bitacora-xp.md` e `historias-usuario.md` registrando el alcance de 45 SP frente a la referencia de 36 como riesgo explícito, el orden por dependencias y la rotación Driver/Navigator; no marcó historias terminadas ni usó terminología fuera de XP.

- HU-28 (Iteración 4): Codex apoyó las fases ROJO/VERDE y la trazabilidad documental. Antes de programar contrastó la Issue #69 con `historias-usuario.md` confirmando título, prioridad Alta, 5 SP y ambos criterios; inspeccionó la suite para no duplicar escenarios (las reglas del dominio ya tenían pruebas unitarias previas desde HU-06 a HU-19) y detectó las áreas huérfanas reales (`TipoCambio`, `AdministrarTipoCambioService`, `NivelAprobacion`, `AdministrarNivelesAprobacionService`, `ConsultarOfertaService`). Creó cinco clases con 34 casos unitarios con trait `HU-28` (corrigió sobre la marcha validaciones síncronas mal asertadas contra métodos async, cambiándolas a `Assert.ThrowsAsync` para que los fallos reflejaran comportamiento y no artefactos del harness), midió el estado rojo real del criterio de cobertura con coverlet (Application 52.93 %, Domain 70.90 %) y verificó tras el incremento 82.68 % / 89.29 %, 34/34 focalizadas, 267 verdes en la solución completa y formato sin diferencias. Documentó honestamente que el ROJO no falló pruebas —el comportamiento ya existía y el criterio admite pruebas concurrentes— sino el umbral de cobertura, y que CI quedó verde también en esa fase; reportó sin ocultar la discrepancia entre la rama prevista (`iteracion-4/hu-28-pruebas-unitarias-dominio`) y la real (`iteracion-4/hu-28-cobertura-pruebas`), la inversión de la pareja planificada según autoría real de commits y que el umbral automatizado en CI corresponde a HU-35. En la documentación actualizó `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo contrastando cada dato con los commits `c0322b9` y `285972e`, sus checks de CI consultados en la API pública de GitHub y las ejecuciones locales; no marcó criterios ni cerró la Issue #69.

- HU-29 (Iteración 4): la IA apoyó la fase REFACTOR y la trazabilidad documental sobre los commits ROJO/VERDE aportados por la pareja (`8a62387` y `ba17ba0`). Contrastó la Issue #70 con `historias-usuario.md` confirmando título, prioridad Alta, 5 SP y los tres criterios; verificó el estado verde previo mediante CI del PR #81 (`failure` esperable en el ROJO, `success` en el VERDE) e inspeccionó la infraestructura previa para no duplicar escenarios ni fixtures. En REFACTOR aplicó una única mejora real sin comportamiento nuevo: deduplicar la creación de la licitación de prueba extrayendo `NuevaLicitacion(codigo, fechaCierre)` para que el caso de concurrencia dejara de repetir literales; evaluó y descartó otros candidatos por ser especulativos o tocar pruebas de otras historias. Diagnosticó y reportó sin ocultar una incidencia ambiental: Smart App Control de Windows bloqueó temporalmente la carga de ensamblados recién compilados (`0x800711C7`, afectando también suites previamente verdes); tras reconstruir, verificó filtro HU-29 3/3, suite completa 270 verdes y formato sin diferencias. Reportó como candidatos a Issues separadas —sin ocultarlos— la discrepancia entre rama prevista y real, la inversión de la pareja planificada según autoría, la incidencia SAC del entorno y el acoplamiento de la aserción de versión a la imagen `postgres:16-alpine`. Actualizó `docs/pruebas.md`, `docs/modelo-datos.md`, `docs/modulos/persistencia.md`, `docs/bitacora-xp.md` y este archivo contrastando cada dato con commits, checks reales de GitHub consultados por API y ejecuciones locales; no marcó criterios ni cerró la Issue #70.

- HU-30 (Iteración 4): Codex apoyó las fases ROJO/REFACTOR y la trazabilidad documental sobre el VERDE aportado por la pareja (`c2ae985`). Antes de programar contrastó la Issue #71 con `historias-usuario.md` confirmando título, prioridad Alta, 8 SP, pareja y ambos criterios, e inspeccionó las suites previas para no duplicar escenarios (el flujo ya estaba cubierto a nivel HTTP con `WebApplicationFactory`; faltaba el navegador real). Creó `tests/Licitaciones.E2ETests` (Playwright 1.62.0, Testcontainers.PostgreSql 4.13.0) con la fixture `LicitacionesE2EFixture` —PostgreSQL `postgres:16-alpine` más la aplicación como proceso real en un puerto libre de loopback— y ocho pasos ordenados con trait `HU-30`, confirmando el rojo por comportamiento ausente (5 fallidas / 3 superadas: sin botón publicar, sin panel de mejor oferta, sin selector CRC/USD y ofertas rechazadas en Borrador). En VERDE verificó la implementación aportada por Tiffany: acción MVC de publicación, moneda y mejor oferta en el listado web de ofertas, formato USD y paso Playwright en `ci.yml`; filtro HU-30 8/8, suite completa en verde y CI `success` (`32758023420`) tras el rojo esperable (`32745900834`). En REFACTOR deduplicó los helpers de los ocho pasos (+38/−28) sin tocar producción (commit local `55d97c0`, sin push ni CI); reportó como candidatos a Issues separadas —sin ocultarlos— la rotación parcial de roles respecto de la pareja planificada y la desviación del enfoque sugerido en la Issue (proceso real con Testcontainers en vez de Docker Compose dentro de CI). Actualizó `docs/pruebas.md`, `docs/bitacora-xp.md`, `docs/modulos/interfaz-web.md` y este archivo contrastando cada dato con commits, ejecuciones de CI consultadas en la API pública de GitHub y ejecuciones locales; no marcó criterios ni cerró la Issue #71.

- HU-31 (Iteración 4): la IA apoyó las fases ROJO y REFACTOR, la propuesta de texto del PR y la trazabilidad documental sobre el VERDE aportado por la pareja (`177039c`). Antes de programar contrastó la Issue #72 con `historias-usuario.md` confirmando título, prioridad Alta, 3 SP y los tres criterios, e inspeccionó las suites previas para no duplicar escenarios (ninguna cubría Dockerfile ni endpoints de infraestructura). Creó `DockerfileTests` (cinco unitarias de contrato del archivo) y `SaludHttpTests` (HTTP `/health` contra PostgreSQL real), confirmando el rojo por comportamiento ausente (5 fallidas por Dockerfile inexistente y 1 por 404) y corrigiendo errores de compilación propios antes de declarar el rojo. En REFACTOR, con la suite previamente verde, extrajo `RaizRepositorio.Obtener()` a `Licitaciones.UnitTests/Common` deduplicando el localizador de raíz entre `DockerfileTests` y `DocumentacionApiMarkdownTests`, sin cambios en producción ni comportamiento nuevo; verificó build, las suites focalizadas y la solución completa tras el cambio (284 verdes). Reportó como candidatos a Issues separadas —sin ocultarlos— la ausencia de evidencia real de `docker build`/`docker run` del contenedor final y que CI aún no construye imágenes. Actualizó `docs/docker.md`, `docs/api.md`, `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo contrastando cada dato con los commits reales, las ejecuciones de CI `32764601886` (rojo esperable) y `32771407873` (`success`) consultadas en la API pública de GitHub, el estado draft del PR #83 y las ejecuciones locales; no marcó criterios ni cerró la Issue #72.

- HU-32 (Iteración 4): la IA apoyó las fases ROJO y REFACTOR y la trazabilidad documental sobre el VERDE aportado por la pareja (`a657d28`). Antes de programar contrastó la Issue #73 con `historias-usuario.md` confirmando título, prioridad Alta, 3 SP y los tres criterios, e inspeccionó las suites previas para no duplicar escenarios (ninguna cubría `compose.yaml` ni `docs/docker.md`). Creó `ComposeFileTests` (seis unitarias de contrato del archivo, con servicios nombrados `app` y `db` según las notas técnicas de la Issue, aceptando `compose.yaml` o `docker-compose.yml`) y `DocumentacionDockerMarkdownTests`, confirmando el rojo por comportamiento ausente (7 fallidas: solo existía el servicio `postgres`, sin servicio `app`, sin sección `build`, sin `depends_on`, sin mecanismo de migraciones y documentación sin `up --build`) tras corregir errores propios de compilación (una llave sin escapar en cadena interpolada y `\w` en cadena no verbatim). En REFACTOR, con la suite previamente verde (291 pruebas), unificó la extracción de secciones YAML sustituyendo la regex multilinea de `volumes:` por el escaneo por líneas `SeccionSuperior` y extrajo el helper compartido `Lineas`; descartó extraer un helper común del bloque de migraciones al arranque entre Web y API por su asimetría intencional de valores por omisión; reportó como candidatos —sin ocultarlos— esa unificación como posible Issue separada, la ausencia de evidencia real de `docker compose up --build` contra Docker Desktop y la introducción desactualizada de `docs/docker.md`, corregida en esta fase documental; verificó filtro 7/7, suite completa 291 verdes y formato sin diferencias. Actualizó `docs/docker.md`, `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo contrastando cada dato con los commits reales, las ejecuciones de CI `32782114673` (rojo esperable) y `32784449582` (`success`) consultadas en la API pública de GitHub, el estado draft del PR #84 y el commit local `51ea07d` sin push ni CI; no marcó criterios ni cerró la Issue #73.

- HU-33 (Iteración 4): la IA apoyó la fase ROJO, la propuesta de texto del PR y la trazabilidad documental sobre el VERDE aportado por la pareja (`f775f63`). Antes de programar ejecutó `gh issue view 74` y contrastó la Issue con `historias-usuario.md` confirmando título, prioridad Alta, 5 SP, RELEASE 10, pareja y los tres criterios literales; inspeccionó las suites previas para no duplicar escenarios (Despliegue solo tenía pruebas de Dockerfile y Compose) y verificó que `/k8s` existía solo con `.gitkeep`. Creó `KubernetesManifestsTests` (seis unitarias de contrato: cuatro manifiestos, tres probes, resources con requests/limits, credenciales vía `secretKeyRef`, rechazo de contraseñas hardcodeadas en los cuatro archivos y Service con `targetPort: 8080`), confirmando el rojo por comportamiento ausente (6 fallidas porque los YAML no existían). En REFACTOR evaluó sin cambios necesarios —descartando anclas YAML por legibilidad y unificar helpers `Leer*` por generalización especulativa— y descartó como falsa alarma una supuesta corrupción de caracteres en comentarios de configmap/secret (solo era el renderizado de la consola; la lectura UTF-8 directa mostró el contenido correcto); verificó filtro 6/6 y suite completa 297 verdes. Reportó como candidatos a Issues separadas —sin ocultarlos— la falta de evidencia real de `kubectl apply` contra un clúster, la rama real distinta de la prevista (`iteracion-4/hu-33-k8s-aplicacion` frente a `-k8s-app`) y el prefijo `feat(k8s)` del VERDE frente al patrón `build(...)` previo. Actualizó `docs/kubernetes.md` (vacío hasta esta fase), `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo contrastando cada dato con los commits reales, las ejecuciones de CI `32789338916` (rojo esperable) y `32790252288` (`success`) consultadas en la API pública de GitHub y las ejecuciones locales; no marcó criterios ni cerró la Issue #74.

- HU-34 (Iteración 4): la IA apoyó las fases ROJO y REFACTOR y la trazabilidad documental sobre el VERDE aportado por la pareja (`9992131`). Antes de programar ejecutó `gh issue view 75` y contrastó la Issue con `historias-usuario.md` confirmando título, prioridad Alta, 5 SP, RELEASE 10, pareja, rama prevista —coincidente exactamente con la real— y los tres criterios literales; inspeccionó las suites previas para no duplicar escenarios (solo `KubernetesManifestsTests` cubría los manifiestos, de la aplicación). Creó `KubernetesPostgresqlTests` (cinco unitarias de contrato: StatefulSet con `volumeClaimTemplates` RWO/storage, montaje de `/var/lib/postgresql/data` referenciando el claim, migraciones controladas vía Job/initContainer con patrón de migración sobre líneas no comentadas, réplicas sin auto-migrar y `docs/kubernetes.md` con instrucciones y evidencias), confirmando el rojo por comportamiento ausente (4 fallidas) con una superada legítimamente por la bandera `false` heredada de HU-33 —rojo mixto documentado— y corrigiendo antes un carácter cirílico accidental en un identificador propio. En una primera invocación del REFACTOR detectó que el VERDE aún no estaba publicado (línea base 4 rojas) y se detuvo reportándolo en lugar de implementarlo oculto. En el REFACTOR efectivo deduplicó la regex de `volumeClaimTemplates` (`PatronVolumeClaimTemplates` + helper `RequerirCuerpoDeVolumeClaimTemplates`) y corrigió una errata de comentario («delPV» → «del PV»); descartó anclas YAML entre manifiestos y un helper `Lineas` compartido entre suites por especulativos; verificó filtro 5/5, suite completa 302 verdes y formato sin diferencias. Reportó como candidatos a Issues separadas —sin ocultarlos— la falta de evidencia real de clúster (pods/PVC/logs/conservación tras reinicio) y que CI aún no construye las imágenes usadas por los manifiestos. Actualizó `docs/kubernetes.md`, `docs/pruebas.md`, `docs/bitacora-xp.md` y este archivo contrastando cada dato con los commits reales, las ejecuciones de CI `32793287726` (rojo esperable) y `32794832818` (`success`) consultadas en la API pública de GitHub, el estado draft del PR #86 y los cambios locales; no marcó criterios ni cerró la Issue #75.

## Ejemplos de apoyo y validación humana

| Apoyo de IA | Validación conservada por el equipo |
| --- | --- |
| Comparar contratos HTTP con controladores. | Revisión directa de rutas, DTO, estados y pruebas. |
| Detectar documentación desactualizada. | Contraste con migraciones, configuración y `git log`. |
| Sugerir comandos reproducibles. | Ejecución local de build y pruebas. |
| Ayudar a describir ciclos TDD. | Confirmación con la secuencia de commits rojo, verde y refactorización. |

La IA no define por sí sola historias, estimaciones, aceptación ni autoría. Las decisiones y la integración permanecen bajo responsabilidad de Seidy y Tiffany. No se deben enviar credenciales, secretos ni datos personales a herramientas externas, y ningún resultado se acepta sin contrastarlo con código, pruebas o historial verificable.
